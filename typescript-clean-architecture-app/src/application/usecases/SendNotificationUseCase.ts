class SendNotificationUseCase {
  async execute(productId: string, price: number) {
    // Lógica para enviar notificaciones por correo electrónico o telegrama
  }
}

export default SendNotificationUseCase;