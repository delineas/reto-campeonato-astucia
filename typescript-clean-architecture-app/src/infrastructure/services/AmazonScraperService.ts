class AmazonScraperService {
  async getPrice(productId: string): Promise<number> {
    // Lógica del scraper para obtener el precio de Amazon
    return 100; // Precio simulado
  }
}

export default AmazonScraperService;